// This component is no longer used as the application now loads directly into the main dashboard.
// It can be safely removed from the project.

const LoginScreen = () => null;

export default LoginScreen;
